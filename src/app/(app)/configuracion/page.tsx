import { Card } from "@/components/ui/Card";

// Placeholder — family management (invite your papá, roles, etc.) lands here next.
export default function ConfiguracionPage() {
  return (
    <div className="mx-auto max-w-2xl space-y-4 px-4 py-5 sm:px-6 sm:py-8">
      <h1 className="text-2xl font-bold tracking-tight sm:text-3xl">Configuración</h1>
      <Card>
        <p className="text-sm text-ink-secondary">
          Aquí va la gestión de tu familia (invitar a tu papá, ver miembros) y tus categorías. Pendiente de construir.
        </p>
      </Card>
    </div>
  );
}
